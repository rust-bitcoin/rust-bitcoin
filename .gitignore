# .gitignore for rust-bitcoin repository workspace.

# Ignore root lockfile, but track subdirectories for tooling.
/Cargo.lock

# Build artifacts
target

# Test artifacts
mutants.out*
fuzz/coverage

# Local tooling (rbmt)
Cargo.lock.backup
